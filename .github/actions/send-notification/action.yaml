name: Insurance Notification
description: Send Insurance CI notifications by email

inputs:
  pipeline_name:
    required: true
  repository_name:
    required: true
  failure_reason:
    required: true
  author:
    required: true
  smtp_username:
    required: true
  smtp_password:
    required: true
  mail_from:
    required: true
  mail_to:
    required: true
  workflow_url:
    required: true
  report_url:
    required: true

runs:
  using: composite
  steps:

    - name: Send email notification
      uses: hilarion5/send-mail@v1
      with:
        smtp-server: smtp.hostinger.com
        smtp-port: 587
        smtp-secure: false
        username: ${{ inputs.smtp_username }}
        password: ${{ inputs.smtp_password }}
        from-email: "Insurance CI Alerts <${{ inputs.mail_from }}>"
        to-email: ${{ inputs.mail_to }}
        subject: "[Insurance CI] ${{ inputs.pipeline_name }} failed for ${{ inputs.repository_name }}"
        html: |
          <div style="padding:24px;background:#f4f7fb;font-family:Arial,sans-serif;">
            <h2 style="margin:0 0 12px;color:#b91c1c;">Insurance pipeline failure</h2>
            <p style="margin:0 0 16px;color:#334155;">
              A workflow in the ${{ inputs.repository_name }} repository needs attention.
            </p>

            <p><b>Pipeline:</b> ${{ inputs.pipeline_name }}</p>
            <p><b>Repository:</b> ${{ inputs.repository_name }}</p>
            <p><b>Triggered by:</b> ${{ inputs.author }}</p>
            <p><b>Failure summary:</b> ${{ inputs.failure_reason }}</p>

            <br/>

            <a href="${{ inputs.report_url }}"
               style="padding:12px 18px;background:#2563eb;color:#fff;text-decoration:none;border-radius:6px;">
               Open Report
            </a>

            <br/><br/>

            <a href="${{ inputs.workflow_url }}"
               style="padding:10px 14px;background:#111;color:#fff;text-decoration:none;border-radius:6px;">
               Open Workflow Run
            </a>
          </div>